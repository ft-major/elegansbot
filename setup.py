from setuptools import setup, find_packages

setup(
    name="elegansbot_conan",  # Your project name
    version="0.1",            # Initial version
    packages=find_packages(), # Automatically find all packages
)
